services:
  main:
    build: .
    container_name: diary
    network_mode: host
    restart: always
    volumes:
      - /home/exenifix/diary-data:/app/data
    environment:
      - DATABASE=diary
      - USER=diary
      - PASSWORD
      - TOKEN
